/*
 * ManageClasses Messages
 *
 * This contains all the text for the ManageClasses component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ManageClasses';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ManageClasses component!',
  },
});
